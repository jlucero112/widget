---
- name: --- SHOW SYSTEM DATE ---
  hosts:
    - localhost
  connection: local
  gather_facts: yes

  tasks:
    - name: --- SHOW DATE AND TIME ---
      debug:
        var: ansible_date_time.iso8601
